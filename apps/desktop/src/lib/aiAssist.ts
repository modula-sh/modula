import { Sparkles } from "lucide-react";

/** Every user-facing string and the icon for the "Use AI" field assist. */
export const AI_ASSIST_LABEL = "Use AI";
export const AI_ASSIST_ICON = Sparkles;
export const AI_ASSIST_TITLE = "Generate with AI";
export const AI_ASSIST_PLACEHOLDER =
  "Describe what to write, e.g. “a Jira-style description for adding dark mode”";
export const AI_ASSIST_SUBMIT_LABEL = "Generate";

/** Shared by every field so the provider pick persists app-wide. */
export const AI_ASSIST_PROVIDER_KEY = "modula.aiAssist.provider";
